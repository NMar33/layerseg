# Colab: torch/torchvision pre-installed, only need additional deps
# Use >= ranges to avoid conflicts with Colab's preinstalled packages
numpy>=1.26
opencv-python>=4.8
Pillow>=10.0
matplotlib>=3.7
click>=8.1
marshmallow>=4.0,<5
marshmallow-dataclass>=8.7,<9
joblib>=1.3
PyYAML>=6.0
reportlab>=4.0
